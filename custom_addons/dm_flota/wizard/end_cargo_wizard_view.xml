<?xml version="1.0" encoding="UTF-8" ?>
<odoo>
        <record id="dm_flota_end_cargo_wizard_view_form" model="ir.ui.view">
            <field name="name">dm.clota.end.cargo.wizard.view.form</field>
            <field name="model">end.cargo.wizard</field>
            <field name="arch" type="xml">
                <form string="End cargo">
                    <group>
                        <field name="transport_id" invisible="1"/>
                        <field name="vehicle_id" invisible="1"/>
                        <field name="internal_note" string="Add final note"/>
                        <field name="add_service_log"/>
                    </group>
                    <footer>
                        <button name="confirm" string="Submit" type="object" class="btn-primary"/>
                        <button string="Cancel" class="btn-secondary" special="cancel"/>
                    </footer>
                </form>
            </field>
        </record>

        <record id="dm_flota_end_cargo_wizard_view_action" model="ir.actions.act_window">
            <field name="name">End cargo</field>
            <field name="type">ir.actions.act_window</field>
            <field name="res_model">end.cargo.wizard</field>
            <field name="view_mode">form</field>
            <field name="view_id" ref="dm_flota_end_cargo_wizard_view_form"/>
            <field name="target">new</field>
        </record>
</odoo>
